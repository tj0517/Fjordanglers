// Campaign definitions are now stored in the `ad_campaign_defs` Supabase table
// and managed through the UI. This file is kept only for shared type references.

export type Platform = 'google_ads' | 'meta'
