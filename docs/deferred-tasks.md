# Deferred tasks

Things noticed during a task and deliberately not done there. One line each: what,
where, why it matters, which task noticed it. Promote to `docs/tasks/` when scheduled.

| Noticed in | What | Where | Why it matters |
|---|---|---|---|
| FA-1.02 | `audit_log` pisany triggerem `audit_trigger_fn` z `guides` i `guide_images`; żaden `src/` go nie czyta. Decyzja: zostaje, rozważyć w etapie 4 razem z `guide_images` | `public.audit_log`, `audit_trigger_fn` | nie dropować bez decyzji o potrzebie operacyjnej |
| FA-1.02 | `public.offers` (14 wierszy, era ekspedycji) koliduje nazwą z nową tabelą `offers` z FA-1.12 | `public.offers` | rozstrzygnąć w FA-1.12 — rename albo drop (dane: era ekspedycji, brak kodu-czytnika) |
| audit 2026-08-31 | ~~`robots.ts` disallows ghost routes `/account/`, `/book/`, `/invite/`~~ — **closed before FA-1.08**: `grep -n "account/\|book/\|invite/" src/app/robots.ts` → brak trafień, `ls -d src/app/{account,book,invite}` → „No such file or directory". Zamknięte wcześniejszą paczką (FA-1.06/1.07), nie przez FA-1.08; FA-1.08 tylko potwierdziło odczytem | `src/app/robots.ts` | closed |
| audit 2026-08-31 | `docs/01-05` duplicated in `head/` with two diverged copies | `docs/`, `head/` | one source of brand truth; fold into `docs/brand/` |
| audit 2026-08-31 | `.claude/settings.local.json` holds live secrets | `.claude/` | O-11 |
| audit 2026-08-31 | `review-media` bucket has no policy migration | Supabase storage | record in stage-1 baseline |
| audit 2026-08-31 | `guides.iban` stored plaintext (`field-encryption.ts` never called) | `guides` | drop the column in stage 4; until then do not add more |
| FA-0.04 | `next build` skips env validation entirely (`NEXT_PHASE === 'phase-production-build'` branch returns raw `process.env` cast as `Env`) — so the type system promises `boolean` for `AI_AUTO_REPLY_ENABLED` (and every other field) but the actual value at that phase is whatever string Vercel injects, unvalidated. Both current reads of the flag are in request handlers (not module scope), so runtime is safe today — but any future module-level read of `env` would silently get a raw string instead of the typed value, defeating the whole point of this task's fix | `src/lib/env.ts:149-152` | not fixed here — out of scope per FA-0.04; needs its own task if a module-level env read is ever added |
| FA-0.02 | ~~Duplicate breadcrumb on the legacy trip editor~~ — page deleted in FA-1.06 | — | closed |
| FA-0.07 | `schema_migrations` na produkcji kończy się na `20260708163723`, kilkanaście migracji po tej dacie weszło poza `db push` | `supabase/migrations`, prod | `db push` zakazany do FA-1.01, która musi najpierw pogodzić historię przez `migration repair` |
| FA-1.01 | `agent-guard.sh` to hook `PreToolUse` harnessu (`.claude/settings.json`, matcher `Bash`), nie git hook: uruchamia się **przed** powłoką, czyta `tool_input.command` jako tekst (`grep -Eiq` po wzorcach) i `FA_ALLOW_PROD` z **własnego** środowiska (linia 9). Prefiks `FA_ALLOW_PROD=1 cmd` trafia tylko do tekstu polecenia, a proces `git`/`supabase` z tą zmienną nigdy nie powstaje, bo hook kończy z `exit 2` wcześniej — odblokowanie per polecenie **nie istnieje i przy tej architekturze nie może istnieć**. Komunikat hooka („rerun with FA_ALLOW_PROD=1 prefixed") obiecuje więc ścieżkę, której nie ma; każdy agent traci na tym wywołanie i opisuje guarda jako „zepsutego". W fazie B FA-1.01 hook był **tymczasowo zmodyfikowany i przywrócony** — precedens obejścia guarda, ma pozostać widoczny. Zdiagnozowane 5 IX 2026 (FA-0.05, test (a) payload z prefiksem → `exit=2`, (b) zmienna w env samego hooka → `exit=0`) | `scripts/agent-guard.sh` linie 9, 27–28; `.claude/settings.json` `hooks.PreToolUse` | zadanie S: naprawić **komunikat** („nie ma odblokowania per polecenie; to polecenie wykonuje człowiek w swoim terminalu"), a nie mechanizm. **Nie** wprowadzać rozpoznawania prefiksu w tekście polecenia (agent wystawiałby zgodę sam sobie). **Nie** ustawiać `FA_ALLOW_PROD` w env sesji ani w `settings.json` `env` (wyłącza guarda całkowicie, także dla `db push` na produkcję). Blokada `git push --force-with-lease` z 5 IX była poprawna — force push należy do §3 i wykonuje go człowiek; dopisać to zdanie do `docs/05-agent-operations.md` §3 |
| FA-1.06 | ~~FA-0.05 (gałąź `feat/inquiry-source-and-utm`, WIP commit `d9209f7a`) było `blocked` na 23 plikach z tabelami `archive`~~ — załatwione w FA-1.06; FA-0.05 zrebase'owane na `main` 5 IX 2026, typy zregenerowane z lokalnej bazy, status `review` | `docs/tasks/FA-0.05.md` | closed |
| FA-1.06 | Publiczny lejek zgłoszeń przewodników nie istnieje po FA-1.06 (usunięty świadomie: `archive.leads` = 0 wierszy, kanał martwy, pozyskiwanie idzie wychodząco). `/guides/apply` → 301 na `/guides`. Gdy będzie potrzebny, budujemy go na `guide_intake_forms`, a nie odtwarzamy `/guides/apply` — audyt 31 VIII §(c).3 o dwóch nieskomunikowanych lejkach nadal obowiązuje | `next.config.ts` (redirect), `docs/audit/rebuild-audit-db-aug-2026.md` | decyzja produktowa tj z 5 IX 2026 |
| FA-1.06 | ~~8 miejsc, które rozwiązywały tytuł/slug/cenę tripa po `inquiries.trip_id` z `archive.experiences`, wycięte (D2 opcja A): maile, nazwa produktu Stripe, `/admin/inquiries` lista i karta, `/dashboard/trips`, agent AI, strona recenzji dostają `'Your trip'` / `'—'`. `sendDepositLink` bez `offer_deposit_eur` zwraca błąd zamiast liczyć z ceny listowej~~ — załatwione w FA-1.09: jeden helper `src/lib/inquiries/experience-lookup.ts` (`experience_page_id`, potem `trip_id`), wszystkie miejsca przepięte | `src/actions/inquiries.ts`, `src/actions/reviews.ts`, `src/actions/ai.ts`, `src/lib/ai/inquiry-agent.ts`, `src/app/admin/inquiries/{page,[id]/page}.tsx`, `src/app/api/webhooks/stripe-deposit/route.ts`, `src/app/dashboard/trips/page.tsx` | closed (FA-1.09) |
| FA-1.06 | ~~`InquiriesClient.tsx` / `InquiriesCalendar.tsx` przyjmują `tripMap`/`slugMap`/`countryMap`, które od FA-1.06 są zawsze puste~~ — załatwione w FA-1.09: mapy zasilone jednym zapytaniem (`getInquiryExperiences`), kluczowane id zapytania; wycięcie propsów i filtra po kraju zostaje dla FA-1.08 | `src/app/admin/inquiries/Inquiries{Client,Calendar}.tsx` | closed (FA-1.09) |
| FA-1.06 | ~~Kompilujący się martwy kod, którego nie ruszyłem (poza brzegiem zadania): `src/components/trips/experience-location-map.tsx` (0 importerów); w `src/types/index.ts` `IcelandicFormConfig`, `INQUIRY_PRESET_FIELDS`, `InquiryCustomField`, `Difficulty`, `LocationSpot` (po usunięciu edytora bez użycia); `handleAccountUpdated` w `api/stripe/webhook` (Stripe Connect, produkt nie ma Connect — ADR-0001); `CACHE_TAG_EXPERIENCES` (jeden użytkownik)~~ — **closed FA-1.07**: `experience-location-map.tsx`, `Difficulty`, `IcelandicFormConfig`, `INQUIRY_PRESET_FIELDS`, `InquiryCustomField` i `handleAccountUpdated` usunięte. **Korekta listy:** `LocationSpot` (importują `ExperiencePageForm.tsx`, `location-picker-map.tsx`, `src/actions/experience-pages.ts`) i `CACHE_TAG_EXPERIENCES` (`dashboard.ts` + 5 miejsc w `queries.ts`) mają realnych użytkowników — zostają | j.w. | closed (FA-1.07); reszta paczki → wiersz „FA-1.07 → FA-1.08” niżej |
| FA-1.06 | 78 `as any` w 14 plikach zostaje — nie wynikają z braku tabel: `Json` kolumny `inquiries.offer_*` przyjmują `OfferQuestion[]`/`ScheduleEntry[]`/`object` (`src/actions/inquiries.ts`), `inquiry_trip_details` rzutowane na lokalny typ (`admin/inquiries/[id]`, `dashboard/trips/[id]`), `party_size`/`angler_country` nullable (`dashboard/trips`, `getInquiryConfirmation.test.ts`), `unmatched_messages.raw_payload` (`messages.ts`, `unmatched/page.tsx`), `lead_messages` insert w webhookach `whatsapp`/`email-inbound`, Leaflet (`L as any`, `Control.Draw`), helper `any()` w `guide-forms.ts` | `git grep -l "as any" src` | usunięcie = typowanie kolumn `Json` przez `Database['public']['Tables']['inquiries']['Insert']` + Zod na granicy; osobne zadanie M |
| FA-1.06 | ~~`pnpm lint` 42 błędy / 60 ostrzeżeń (przed: 62/65) — reszta sprzed tego zadania: surowe apostrofy w szablonach maili, `image-crop.tsx` ref-during-render, parse error `whatsapp-bridge/poll-emails.mjs`, nieużywane `eslint-disable`~~ — **closed FA-1.08**: `pnpm lint` → **0 błędów / 66 ostrzeżeń** (wejście: 35/75, `docs/proofs/FA-1.08-lint-before.txt` → `-after.txt`). Kryterium zadań przestaje brzmieć „nie gorzej niż `main`" — od FA-1.08 jest „zielony", bo krok `lint` w `ci.yml` stracił `continue-on-error` | `eslint.config.mjs`, `.github/workflows/ci.yml` | closed (FA-1.08) |
| FA-1.06 | FA-1.09 („legacy edytor `experiences` poza nawigacją; gorące akcje na `experience_pages`") traci pierwszą połowę przedmiotu — edytor usunięty w całości w FA-1.06 (D1) | `docs/tasks/INDEX.md` FA-1.09 | tj: `dropped` albo zawęzić do „gorących akcji" |
| FA-1.06 | Produkcja nieodczytana w tym zadaniu: `SUPABASE_DB_PASSWORD` nie było w env sesji, MCP Supabase `Unauthorized`, CLI 2.75 nie ma `db query`. Decyzja tj 5 IX: odczyt prod = pierwszy punkt zadania-helpera z D2; hasło ustawiane świadomie, na jedno polecenie | `docs/05-agent-operations.md` §4 | dopisać do runbooka sesji |
| FA-1.06 | Martwe klucze w `.claude/settings.local.json` sekcja `env` przesłaniają działające logowania: `GITHUB_TOKEN` → `gh` zwraca `HTTP 401: Bad credentials` (FA-1.06, PR musiał być otwarty ręcznie), `SUPABASE_ACCESS_TOKEN` → CLI Supabase ignorowało keychain (FA-1.01, workaround `SUPABASE_ACCESS_TOKEN=""`). Dwa razy zablokowało pracę Trzeci raz w FA-0.05 (5 IX): `supabase gen types --project-id …` → `{"message":"Unauthorized"}`, działa tylko z prefiksem `SUPABASE_ACCESS_TOKEN=""` | `.claude/settings.local.json` `env` | usunąć oba klucze z `env` — naprawia oba objawy (`gh auth login` i keychain Supabase działają). Wchodzi do FA-0.09 (O-11) albo osobny S; zgłoszone też przez FA-0.05 |
| FA-0.05 | `pnpm supabase:types` **zeruje** `src/lib/supabase/database.types.ts`, gdy komenda padnie: przekierowanie `>` otwiera (i obcina) plik zanim `supabase gen types` się uruchomi; przy pustym `$SUPABASE_PROJECT_ID` CLI kończy na `flag needs an argument: --project-id` i w repo zostaje pusty plik typów (zdarzyło się 5 IX, przywrócone z gita) | `package.json` → `scripts.supabase:types` | generacja do pliku tymczasowego, `mv` po sukcesie, guard na pusty `$SUPABASE_PROJECT_ID` (`${SUPABASE_PROJECT_ID:?}`) — FA-1.11, ten sam skrypt idzie do CI |
| FA-0.05 | Zdalny (`gen types --project-id`) i lokalny (`gen types --local`) generator typów dają **inny szkielet pliku przy identycznym schemacie**: zdalny dodaje nagłówek `__InternalSupabase { PostgrestVersion }`, domyślnie pomija `graphql_public`, nawiasuje typy warunkowe w helperach `Tables<>`/`TablesInsert<>`/`TablesUpdate<>`/`Enums<>`/`CompositeTypes<>` i kończy bez newline'a — 12 hunków, 0 w `Database['public']` (5 IX, CLI 2.75.0 po obu stronach). Plik w repo pochodzi z `--local` od FA-1.06 | `src/lib/supabase/database.types.ts`, `package.json` `supabase:types` | CI musi wybrać jeden kanoniczny generator (rekomendacja: `--local` po `migration up` — bez sekretów produkcji, a przy okazji sprawdza, że migracje się aplikują), inaczej `gen types` + `git diff --exit-code` w pipeline zawsze będzie czerwony — FA-1.11; kryterium parytetu do tego czasu: „sekcja `Database.public` identyczna" |
| FA-0.11 | Kubełki filtra ceny na `/trips` mają zaszyty `€` na sztywno: `{ label: 'Under €150', ... }` itd. | `src/app/trips/filters.tsx:18-21`, `src/app/trips/filters-modal.tsx:15-18` | inny problem niż ten task — `/trips` miesza waluty (Nordics=EUR, Patagonia/NZ=USD/NZD) w jednej liście, więc kubełek `€150–300` nie ma jednoznacznej waluty; wymaga decyzji produktowej (np. kubełki bez symbolu walutowego, albo filtr per-waluta), nie mechanicznej podmiany na `formatPrice` |
| FA-0.11 | `src/app/admin/experiences/page.tsx:113` (`from €${row.price_from}`) ma ten sam bug co ten task, ale w adminie — poza zakresem tego PR-a razem z resztą modułu admina (`offers/[token]`, `admin/inquiries`) | `src/app/admin/experiences/page.tsx:113` | mechaniczna podmiana na `formatPrice({ priceFrom: row.price_from, priceType: row.price_type, currency: row.currency })`, jeśli select ma te kolumny — zadanie S |
| FA-0.12 | `src/app/layout.tsx:28/44/50` — domyślny `<title>`/OG title mówi „Nordic Countries", `keywords` (linie 33-37) wymienia „fishing guide Scandinavia"/„Nordic fishing guide"; homepage (`src/app/page.tsx:89/114/149`) i `home-faq.tsx` mówią wyłącznie o Nordyku | `src/app/layout.tsx`, `src/app/page.tsx`, `src/components/home/home-faq.tsx` | odczyt tj z 5 IX: to copy i decyzja pozycjonowania strony głównej, nie konfiguracja — nie naprawiać w FA-0.12. FA-0.12 dodało `keywords` per-page tylko w `experiences/[slug]` i `/trips`, żeby przestały dziedziczyć te słowa kluczowe; layout.tsx samo w sobie zostaje |
| FA-0.12 | `src/components/ui/contact-expert-button.tsx:10` — link WhatsApp ma wpisany w URL tekst „a guided fishing trip in Scandinavia", widoczny dla klienta z reklamy Patagonii/NZ, który klika ten przycisk na dowolnej stronie | `src/components/ui/contact-expert-button.tsx:10` | copy, nie konfiguracja — zadanie S: parametryzować tekst po `country`/regionie tam, gdzie przycisk jest wołany z kontekstem strony |
| FA-0.12 | Podwójny sufiks `meta_title` w danych (`… \| FjordAnglers \| FjordAnglers`) — FA-0.12 naprawiło **renderowanie** (`title: {absolute}` gdy `meta_title` już kończy się sufiksem), ale nie dane u źródła. Dokładna lista slugów z `select slug, meta_title from experience_pages where meta_title ilike '%fjordanglers%'` niewykonana w tej sesji (brak poświadczeń Supabase) — zależna od odczytu tj | `experience_pages.meta_title` | tj: uruchom SELECT, dopisz listę tutaj; UPDATE do usunięcia sufiksu u źródła to osobne zadanie S, nie ten PR |
| FA-0.12 | Spójność zapisu `experience_pages.country` (`'Chile'` vs `'chile'`, `'New Zealand'` vs `'NZ'`) nie zweryfikowana pełnym odczytem produkcji w tej sesji — `getRegionGroup()`/`COUNTRIES.find()` w tym PR-ze dopasowują case-insensitive, więc literówki nie wysypią kodu, ale źle zapisany wiersz nie trafi do żadnej grupy regionu (zniknie z cross-sellu i stopki) zamiast rzucić błąd | `src/lib/countries.ts` `getRegionGroup()` | tj: odczyt `select distinct country from experience_pages order by 1` na produkcji, potwierdź brak wariantów zapisu poza tymi w `COUNTRIES` |
| FA-0.12 | Partnerzy **bez potwierdzonej prowizji** → `status='draft'` — pominięte w FA-0.12, decyzja biznesowa osobno. Lista skorygowana przez tj 5 IX: dotyczy **Flywise** (`flywise-anglers-aysen-lodge-week`, active) i **PRG** (`patagonia-river-guides-argentina`, już draft) — u obu pytanie „czy publikowane stawki zawierają prowizję" zadane dwa razy, bez odpowiedzi. **Natales (`fly-fishing-torres-del-paine-puerto-natales`) NIE jest na tej liście** — warunki handlowe potwierdzone mailem Diego Roblesa z 1 IX (net rates, marża 10–15% dzień / 20% programy, Tier 1); `FA-0.12.md` powstał wcześniej i wymieniał go błędnie | `experience_pages` (produkcja) | tj wykonuje `UPDATE experience_pages SET status='draft' WHERE slug IN ('flywise-anglers-aysen-lodge-week','patagonia-river-guides-argentina')`; Natales zostaje `active` |
| FA-0.13 | ~~`src/components/trips/ExperiencePageWithOptions.tsx` nie ma żadnego importera poza sobą i typem w `TripOptionsAccordion.tsx`~~ — **closed**: plik usunięty wcześniejszą paczką (FA-1.06/1.07); `ls` → „No such file or directory", `grep -rn "ExperiencePageWithOptions" src` → 0. FA-1.08 domknęło ostatni ślad: nieaktualną wzmiankę w docblocku `TripOptionsAccordion.tsx` oraz sam nieużywany komponent `TripOptionsAccordion` (knip) | `src/components/trips/TripOptionsAccordion.tsx` | closed (FA-1.06/1.07 + FA-1.08) |
| FA-0.06 | `getReviewUploadUrl` accepts `contentType` param but does not pass it to `createSignedUploadUrl` — upload content-type is not set on the signed URL, so uploaded files default to `application/octet-stream` | `src/actions/review-media.ts:21` | pass `contentType` to the storage call options — S task |
| FA-0.06 | `revalidateTag` in `dashboard.ts` called with two args `(tag, {})` but Next.js 15 `revalidateTag` signature accepts one — second arg silently ignored today, will break when types updated | `src/actions/dashboard.ts:251-252` | remove the second `{}` argument — S task |
| FA-0.10 | `google-ads-api@24.1.0` crashes in `getGoogleAdsError` (`service.js:112`) on `error.metadata.internalRepr.get(FAILURE_KEY)` because grpc-js changed the internal repr property from `internalRepr` to `_internal_repr`. The partially-decoded `google.rpc.ErrorInfo` (with `reason`, `domain`) is available inside the method before the crash but the TypeError thrown never reaches our try/catch in `fetch-campaigns.ts` — so production logs show the TypeError, not the Google error code. Fix: upgrade `google-ads-api` to a version that handles `_internal_repr`, or patch `Service.prototype.getGoogleAdsError` in `client.ts` to add try/catch that logs `error.reason`/`error.domain` before calling through | `node_modules/google-ads-api/build/src/service.js:112`, `src/lib/google-ads/client.ts` | S task; unblocks seeing actual Google API errors in server logs |
| FA-0.16 | `agent-guard.sh` blokuje `supabase db reset` i `supabase migration repair --local` tak samo jak operacje na produkcji (`apply_migration`, `db push`, `execute_sql` inne niż SELECT). Obie komendy są **lokalne** (lokalny kontener Docker, brak dostępu do prod) i bezpieczne, a guard blokuje je po wzorcu tekstowym bez rozróżnienia lokalny/zdalny. Powiązane: `FA_ALLOW_PROD=1` prefiks **nie odblokowuje per polecenie** (hook czyta własny env, nie tekst komendy — zdiagnozowane w FA-1.01 i zapisane w wierszu wyżej). Skutek: agent musi prosić człowieka o ręczne uruchomienie `db reset`/`repair --local`, tracąc wywołanie i czas. Dodatkowo guard dopasowuje wzorce do **całej treści polecenia**, więc blokuje też `gh pr create`, którego opis jedynie *cytuje* nazwę blokowanego polecenia — nie odróżnia polecenia od tekstu o poleceniu (FA-0.16, 10 IX 2026: opis PR-a #26 wyjaśniający, jak naprawić lokalną bazę, został zablokowany; przeszedł dopiero po przeformułowaniu prozy). Ta sama pułapka dotyczy `git commit -m` z takim opisem w treści | `scripts/agent-guard.sh` wzorce grep `(db reset\|migration.*repair)` | zadanie S: (1) wyłączyć `db reset` i `migration repair --local` ze wzorców guarda (lub dodać wyjątek dla flagi `--local`); (2) zaktualizować komunikat błędu: usunąć wzmiankę o `FA_ALLOW_PROD=1` jako sposobie odblokowania — napisać wprost „nie ma odblokowania per polecenie; uruchom to polecenie ręcznie w swoim terminalu" |
| FA-0.17 | Liczby `20+` (guides) i `7` (countries) na stronie głównej to literały w tablicy w `page.tsx`, nie liczone z `getPlatformStats()` (ta funkcja istnieje w `queries.ts` i jest używana na `/guides`, ale `grep -n "getPlatformStats" src/app/page.tsx` → 0 trafień). Jeśli mają odzwierciedlać stan bazy, potrzebne podpięcie `getPlatformStats()` do strony głównej | `src/app/page.tsx` (karta stats, linie z `stat: '20+'` i `stat: '7'`) | zadanie S: zamienić literały na wywołanie `getPlatformStats()` i wyrenderować liczby dynamicznie |
| FA-0.18 | Agent AI (`inquiry-agent.ts`) klasyfikuje `trip_country` z tekstu wiadomości i może zwrócić `'Other'` — wartość spoza listy `COUNTRIES` w `src/lib/countries.ts`. Backfill FA-0.18 pomija `'Other'` bez normalizacji. Docelowo: ujednolicić listę krajów w prompcie agenta z `COUNTRIES`, albo mapować `'Other'` na `null` i ponownie klasyfikować po przypisaniu wyprawy | `src/lib/ai/inquiry-agent.ts`, `src/lib/countries.ts` | zadanie S; dopiero po FA-0.18 (żeby wiedzieć, ile wierszy z `'Other'` zostaje po backfillu) |
| FA-0.14 | `ExpCard` skopiowany do `src/app/(public)/[destination]/page.tsx` zamiast wyekstrahowany do osobnego pliku — ekstrakcja wymagałaby zmian w `exp-page-map-section.tsx` (out of scope FA-0.14) | `src/app/(public)/[destination]/page.tsx`, `src/app/trips/exp-page-map-section.tsx` | zadanie S: wynieść `ExpCard` i `ExpPage` do `src/components/trips/exp-card.tsx`, zastąpić import w obu miejscach |
| FA-0.16 | **Merge gałęzi zjada jednolinijkowe zmiany statusu w `INDEX.md`.** FA-0.16 ustawiło `todo → review` w commicie `70d18414`; na `main` wpis wrócił do `todo`, bo merge `67a58428 Merge branch 'main' into feat/offer-sla-48h` rozwiązał konflikt na tym wierszu po stronie `main`. W diffie PR-a #24 nie widać tego wcale — `git log -- docs/tasks/INDEX.md` też nie pokazuje `70d18414`, bo zmianę cofnięto w merge'u. Wykryte dopiero przy ustawianiu `done` 10 IX 2026, czyli status był nieprawdziwy przez cały review. Przyczyna źródłowa: ten sam fakt (status zadania) mieszka w dwóch plikach, a szablon zadania wprost każe edytować oba **na gałęzi feature'owej** („Status `todo → review` tu i w `INDEX.md`, w tym samym PR" — sekcja „Gotowe, gdy"), więc każde zadanie gwarantuje sobie konflikt na tym samym wierszu tabeli | `docs/tasks/INDEX.md`, szablon „Gotowe, gdy" w `docs/tasks/FA-*.md` | **decyzja tj — trzy opcje.** (1) **`INDEX.md` generowany, nie pisany ręcznie**: status żyje wyłącznie we frontmatterze `docs/tasks/FA-x.yy.md`, skrypt (`pnpm tasks:index`) przepisuje tabelę, CI pilnuje, że wygenerowany plik = zacommitowany. Usuwa klasę błędu, bo znika duplikat; koszt: skrypt jednorazowo. (2) **Status zmieniany wyłącznie w osobnym PR-ze po merge** — zmiana nigdy nie leży na gałęzi feature'owej, więc nie bierze udziału w konflikcie; zero kodu, ale +1 PR na zadanie i nadal opiera się na dyscyplinie (dokładnie tak zrobiliśmy w PR #28). (3) **Test w CI porównujący `INDEX.md` z frontmatterem plików zadań** — duplikat zostaje, rozjazd wywala pipeline; wykrywa, nie zapobiega, i wymaga CI z FA-1.11. Rekomendacja: (1) jako właściwa naprawa, (3) jako tania proteza, jeśli CI wyląduje wcześniej. Przy (1) i (3) trzeba poprawić też zdanie w szablonie zadania, inaczej dalej każe robić to, co powoduje kolizję |
| FA-0.18 | Dwóch przewodników na produkcji ma `guides.country = ''` (pusty string, nie NULL): `Mika Ahola` (pending) i `Emanuel Cosic` (active). Kolumna jest `NOT NULL`, więc pusty string jest jedyną drogą na "nie wiem"; helper `tripCountryPatchFromGuide` traktuje go jak brak i zostawia `trip_country` NULL | `guides.country` (produkcja), `src/lib/inquiries/trip-country.ts` | zadanie S: uzupełnić kraj tym dwóm przewodnikom i dodać `CHECK (country <> '')`, żeby formularz przewodnika nie mógł znów zapisać pustego |
| FA-0.14 | Dane w `experience_pages`: islandzkie `experience_name` niosą marketingowe przymiotniki („Elite", „Specialized"), a `price_type` miesza `per trip` z `/ person` między krajami — na hubie destynacji widać to obok siebie w jednej siatce, więc rozjazd jest teraz widoczny dla klienta z reklamy | `experience_pages.experience_name`, `experience_pages.price_type` (produkcja) | wprost wypisane w „Poza zakresem" FA-0.14 — osobne zadanie S: przejrzeć tytuły islandzkie i ustalić z tj, które `price_type` są naprawdę `flat`, a które `per_person` |
| FA-0.14 | ~~`src/actions/getInquiryConfirmation.test.ts > returns depositPaidAt: null for an unpaid inquiry` pada na main od co najmniej 5c47b32 (`expected null not to be null`). Test integracyjny parsuje `.env.local`, które wskazuje na **produkcję** `uwxrstbplaoxfghrchcy`. W prodzie nie ma już wiersza z deposit_paid_at = null, który test zakłada~~ — naprawione w FA-0.21: test celuje w lokalny stack, INSERT zakłada pusty lokalny DB | `src/actions/getInquiryConfirmation.test.ts`, `.env.local` | closed — FA-0.21 |
| FA-0.20 | ~~`pnpm test` wstawia wiersze do produkcyjnej tabeli `inquiries` kluczem serwisowym (z pominięciem RLS). `inquiryStatusDefault.test.ts` dołączony w FA-0.20 poszedł za wzorcem z `getInquiryConfirmation.test.ts` — parsuje `.env.local` w `beforeAll` i robi INSERT. Do 15 IX chroniły produkcję martwe klucze legacy; po ich wymianie ta przypadkowa osłona znikła~~ — naprawione w FA-0.21: `.env.test` + bezpiecznik w setupFiles, oba testy usunęły ręczne parsowanie `.env.local` | `src/actions/__tests__/inquiryStatusDefault.test.ts`, `src/actions/getInquiryConfirmation.test.ts` | closed — FA-0.21 |
| FA-0.14 | **Apex zaszyty na sztywno w ośmiu miejscach, a kanoniczną domeną jest `www`** — `https://fjordanglers.com` zwraca 307 na `https://www.fjordanglers.com`, więc mapa witryny zgłasza wyłącznie adresy przekierowujące, a `metadataBase` produkuje takie same URL-e kanoniczne i OG. Wykryte przy odbiorze FA-0.14 (15 IX), gdy trzy nowe huby destynacji weszły do `sitemap.xml` pod apeksem. Dla wyszukiwarek to rozmyty sygnał kanoniczności, akurat gdy na te trasy idzie płatny ruch | `src/app/sitemap.ts:8` (`BASE`), `src/app/layout.tsx:26` (`metadataBase`), `:42`, `:96`, `:101`, `:117`, `:118`, `src/app/robots.ts:21` | zadanie S: jedna stała `SITE_URL = 'https://www.fjordanglers.com'` w `src/lib/`, użyta we wszystkich ośmiu miejscach; sprawdzić w Search Console, czy apex nie jest zgłoszony jako osobna własność |
| FA-0.12 | **Zmiana `experience_pages` nigdzie nie unieważnia cache'u `experiences`.** `revalidateTag(CACHE_TAG_EXPERIENCES)` jest w całym `src/` wołane w jednym miejscu — `updateGuideProfile`, czyli zapis profilu przez przewodnika w `/dashboard`. Żadna akcja admina (publikacja strony doświadczenia, zmiana `status` na `active`) tagu nie zrzuca, więc listy z `unstable_cache` (m.in. stopka, featured) czekają na `revalidate: 300` albo na to, aż przypadkowy przewodnik zapisze profil. Przy celu „3 destynacje live do 30 IX" to leży na ścieżce krytycznej | `src/actions/dashboard.ts:248`, `src/lib/supabase/queries.ts` | zadanie S: wołać `revalidateTag(CACHE_TAG_EXPERIENCES)` w akcjach admina zmieniających `experience_pages` (publikacja, zmiana statusu, edycja treści) |
| FA-0.12 | `/trips` renderuje się przez `createServiceClient()` — publiczna trasa czyta kluczem serwisowym, z pominięciem RLS. Stąd `/trips?country=Chile` pokazywało wyprawy nawet wtedy, gdy anonimowy odczyt był martwy (15 IX). Nie awaria, ale publiczna ścieżka ma pełny dostęp do tabel i nie testuje polityk, na których opiera się reszta strony | `src/app/trips/page.tsx:143` | zadanie S/M: przenieść odczyt na `createPublicClient()`, sprawdzić, czego brakuje w politykach RLS, gdyby coś przestało być widoczne |
| FA-0.12 | Zerwany odczyt wygląda jak pusty wynik: `getActiveDestinationCountries()` (i pozostałe helpery w `queries.ts`) robią `console.error` + `return []`. Przy wyłączonych kluczach legacy strona pokazywała „brak destynacji" zamiast alarmu; lokalnie chodziło tak cztery dni, zanim ktokolwiek zauważył | `src/lib/supabase/queries.ts` (wszystkie `return []` po `error`) | zadanie S: rzucać w dev, raportować do Sentry w prod; pusty wynik z powodu błędu nie może być nieodróżnialny od pustego wyniku z powodu danych |
| FA-0.12 | **Kryteria „Gotowe, gdy" w plikach zadań celują w apex `https://fjordanglers.com`, który zwraca 307** na `https://www.…`. `curl` bez `-L` zapisuje pustą stronę, więc `grep -c` daje 0 i kryterium „przechodzi" bez sprawdzenia czegokolwiek. Przy odbiorze FA-0.12 (15 IX) dało to komplet fałszywych zer | `docs/tasks/FA-*.md`, sekcje „Gotowe, gdy" i „Weryfikacja" | zadanie S: przejrzeć wszystkie pliki zadań pod `curl https://fjordanglers.com`, zamienić na `curl -sSL https://www.fjordanglers.com`; dopisać regułę do `docs/tasks/README.md` |
| FA-0.17 | Literówka w taglinie widocznym na każdej stronie i w meta description: „Each guide is met**and** checked by the founders" — brak spacji | tagline z FA-0.17 (footer + `generateMetadata`) | jednoznakowa poprawka, doklej do dowolnego najbliższego PR-a dotykającego copy |
| — | **Klucze legacy Supabase (anon, service_role) zostały wyłączone 11 IX 2026 09:31 UTC** i nigdzie w `docs/` tego nie odnotowano. `.env.local` tj miał martwy klucz do 15 IX (każdy lokalny odczyt → `Legacy API keys are disabled`), a `SUPABASE_SERVICE_ROLE_KEY` w ogóle w nim nie istniało, mimo że `src/lib/env.ts:37` wymaga go jako `min(1)` — `/trips` wywalało się lokalnie na `supabaseKey is required`. Nowe klucze: `sb_publishable_…` pod `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `sb_secret_…` pod `SUPABASE_SERVICE_ROLE_KEY` (kod zna tylko te nazwy) | `.env.local`, Vercel env, `docs/05-agent-operations.md` | odnotować datę i nowy format kluczy w runbooku; sprawdzić `whatsapp-bridge/.env` i wszystkie środowiska Vercela pod kątem pozostałych kluczy `eyJ…`. Walidacja w `env.ts` przyjmuje dowolny niepusty string — rozważyć wzorzec `sb_(publishable\|secret)_`, żeby martwy klucz legacy wywalał się przy starcie, a nie po cichu przy odczycie |
| FA-0.20 | `inquiry-received-fa.tsx` ma hardcoded `Status: pending` w stopce maila zamiast faktycznego statusu zapytania. Komponent nie ma propsa `status` — wszystkie nowe maile będą poprawne, ale ewentualny re-send na zapytaniu z innym statusem wyświetli `pending` zamiast prawdziwego. Tracing wszystkich callerów i dodanie propsa to osobne zadanie | `src/emails/inquiry-received-fa.tsx:70` | zadanie XS: dodać `status?: string` do `InquiryReceivedFaEmailProps`, użyć `status ?? 'pending'` w treści, przepiąć callery |
| FA-0.19 | `GET /api/cron/offer-sla` nie jest odczytem bez skutków ubocznych — wywołanie z ustawionym `OWNER_EMAIL` i niepustą listą natychmiast wysyła mail alarmowy do skrzynki ("mailed":true). Używanie endpointu do weryfikacji na odbiorze zadania generuje fałszywy alarm. Weryfikacja crona na odbiorze FA-0.19 wysłała mail z 17 zaległymi (po backfillu — spóźniony alarm o sprawach już obsługiwanych) | `src/app/api/cron/offer-sla/route.ts` | zadanie S: dodać tryb dry-run (np. `?dry=1` lub nagłówek) zwracający tylko `{overdue: N}` bez wysyłki maila; albo odnotować w `docs/05-agent-operations.md`, że weryfikacja crona robi się przez SELECT, nie przez curl endpointu |
| FA-1.03 | ~~`pnpm lint` jest czerwony na `main` niezależnie od tego zadania: 40 błędów w plikach, których FA-1.03 nie dotyka~~ — **closed FA-1.08**. 35 błędów na wejściu (FA-1.07 zdjęło 5 razem z usuniętymi plikami) rozbiło się na: 0 zniknęło z martwym kodem, 12 naprawa składniowa (10× `react/no-unescaped-entities`, 1× `prefer-const`, 1× `no-require-imports`), 1 parse error (`whatsapp-bridge/poll-emails.mjs` — plik **nie parsował się w ogóle**, `node --check` czerwony, więc poller PM2 nigdy nie mógł wystartować; naprawa składni, nie wyłączenie katalogu), 22 `react-hooks/*`. Nieużywane dyrektywy `eslint-disable`: `--report-unused-disable-directives` wykryło **cztery** (`scripts/proofs/stubs/next-headers.mts:19`, `src/components/home/bg-video.tsx:16`, `src/components/trips/ExperienceTabLayout.tsx:123`, `src/components/trips/experience-gallery.tsx:266`) — wszystkie usunięte, po zmianie flaga nie zgłasza żadnej. Flaga została na stałe w skrypcie `lint`, żeby ten punkt miał egzekucję. Wyłączenie `whatsapp-bridge/` z lintu **odrzucone**: ukryłoby realną awarię, nie stylistykę | `src/emails/*.tsx`, `whatsapp-bridge/poll-emails.mjs`, `eslint.config.mjs` | closed (FA-1.08) |
| FA-1.03 | ~~Supabase CLI nie znajduje lokalnego stacku: od aktualizacji CLI (16 IX) nazwy kontenerów bierze z `config.toml` `project_id` (`supabase_db_fjordanglers`), a działające kontenery nazywają się `supabase_*_uwxrstbplaoxfghrchcy` (z `supabase/.temp/project-ref`, jak robiło starsze CLI). `supabase status`, `gen types --local` i `db diff --local` kończą się `failed to inspect container health: No such container: supabase_db_fjordanglers`. `migration up --local` działa, bo łączy się po porcie. Obejście użyte w FA-1.03: chwilowa podmiana `project_id` na ref i powrót po komendzie~~ — **closed 17 IX 2026 przez FA-1.11** | `supabase/config.toml:5`, `supabase/.temp/project-ref` | zadanie S — decyzja tj: albo zrestartować stack pod obecnym `project_id` (`supabase stop` + `start`, kontenery dostaną nazwy `*_fjordanglers`), albo na stałe ustawić `project_id = "uwxrstbplaoxfghrchcy"`. Do tego czasu każdy agent traci wywołania na tej samej pułapce; notatka pamięci `local-supabase-stack.md` opisuje starą regułę i jest już nieaktualna. **Rozstrzygnięte:** tj wybrał wariant „zostaw `project_id = \"fjordanglers\"`, przestaw kontenery" (17 IX) — stack zatrzymany i wystartowany od nowa, więc CLI utworzyło go jako `supabase_*_fjordanglers`, zgodnie z `config.toml`. Ref produkcji nie jest nazwą niczego lokalnego. Od tej zmiany `supabase stop/start`, `db diff --local` i `gen types --local` działają bez obejścia — sprawdzone w FA-1.11. Ujawniło się przy tym, że stary rozjazd powodował, że `supabase stop` **nic nie zatrzymywał**, zgłaszając sukces |
| FA-1.03 | `handed_over` istnieje w maszynie stanów i w `StatusChanger`, ale **żadna akcja aplikacji go nie ustawia** — jedyne dojście to ręczny klik admina. Docelowo ustawia go „kontakty wymienione" w wątku (`contacts.exchanged`), które powstaje w FA-1.12 | `src/lib/inquiries/state.ts`, `docs/REBUILD_PLAN.md` załącznik C | nic do zrobienia teraz — pilnować przy odbiorze FA-1.12, żeby przycisk „kontakty wymienione" wołał `transition(handed_over)` w tym samym submit |
| FA-1.03 | **`main` nie ma branch protection.** 16 IX PR #47 (FA-1.03) został zmergowany do `main` zamiast do `stage-1` — GitHub domyślnie proponuje `main` jako bazę. Skutek: kod maszyny stanów wjechał na produkcję (deployment `2lx6ss20e`, alias `www.fjordanglers.com`, Ready 23:12:43) na bazie, która nie ma migracji `20260916201225`/`20260916201226`, więc każdy insert z `/api/inquiries` (status `new`) łamałby `inquiries_status_check`. Okno 23:12–23:19; `SELECT` z produkcji i logi Vercela pokazują zero `POST /api/inquiries` w tym czasie, więc nic nie zginęło. Naprawa: revert `1505c7f3` na `main` | GitHub settings → Branches; `docs/tasks/README.md` („Cykl życia"), `docs/REBUILD_PLAN.md` §8 | **tj ustawia branch protection na `main`**: wymagany review, zakaz merge bez zielonego CI, zakaz force-push. Do czasu FA-1.11 (CI) jedynym zabezpieczeniem jest jawny `--base` przy `gh pr create` — reguła dopisana do `docs/tasks/README.md` |
| FA-1.11 | Nieśledzony katalog `HEAD/` w korzeniu repo (kopia `docs/brand/`: `01-brand-overview.md` … `voice-of-customer.md`, `.DS_Store`, xlsx). Skutek praktyczny: **`git diff <rev> HEAD` przestaje działać** — git zgłasza `ambiguous argument 'HEAD': both revision and filename` i wymaga `--`. Powstało prawdopodobnie z literówki w `git checkout … HEAD` albo `cp -r … HEAD`. Bliźniak wpisu „`docs/01-05` duplicated in `head/`" z audytu 2026-08-31 | korzeń repo, `HEAD/` | S: usunąć katalog (jest ignorowany, więc nie ma go w historii — nic nie ginie) albo złożyć razem z `head/` w `docs/brand/`. Dopóki jest, każdy agent i każdy skrypt porównujący drzewa traci wywołanie na tej samej pułapce |
| FA-1.11 | ~~**OrbStack pada przy `pnpm build` z działającym stackiem Supabase** — dwa razy w dwa dni (16–17 IX 2026). Host ma 8 GB RAM, VM OrbStacka 4 GB (`memory_mib: 4096`, limit potwierdzony: `docker info` → 3,89 GiB; podniesione z 3 GB 17 IX, bo na maszynie stoją dwa stacki Supabase); pełny stack to jedenaście kontenerów (~715 MiB), a `next build` nie ma pułapu sterty (`--max-old-space-size=2048` siedzi w skrypcie `dev`, nie `build`) — zmierzony szczyt RSS procesu głównego 1,44 GiB plus workery~~ — **closed 17 IX 2026** | `docs/05-agent-operations.md` §9, `CLAUDE.md` reguła 11 | Zamknięte sekcją „Lokalne środowisko (8 GB RAM)": stack wyłącznie z listą `-x` (cztery kontenery zamiast jedenastu), build tylko przy zatrzymanym stacku, sprawdzanie Dockera przed komendami na stacku, zakaz dwóch rzeczy ciężkich pamięciowo naraz. Przyczyną, dla której reguła „zatrzymaj stack" wcześniej nie działała, był rozjazd `project_id` (wpis wyżej) — `supabase stop` zgłaszał sukces i nie zatrzymywał niczego |
| FA-1.11 | **CI nie wykrywa dryfu produkcji wobec repo.** Job `db` sprawdza wyłącznie, że migracje aplikują się na czysto i że `database.types.ts` zgadza się ze schematem zbudowanym z tych migracji — czyli repo względem samego siebie. `supabase db diff --linked` (repo względem produkcji) był świadomie poza zakresem FA-1.11: wymaga hasła bazy w secrets, osobna decyzja tj | `.github/workflows/ci.yml` job `db`, `docs/03-conventions.md` sekcja „CI" | **Przed pushem paczki `stage-1`: ręczny `supabase db diff --linked` jako bramka STOP.** Bez tego nikt nie wie, czy produkcja nie rozjechała się z migracjami między FA-1.01 a dniem pushu |
| FA-1.12 | ~~**Seed fixtures używają UUID z cyfrą wersji `0`**~~ — **closed FA-1.05**: `supabase/seed.sql` utworzony w FA-1.05 z UUID v4 od początku (`xxxxxxxx-xxxx-4xxx-8xxx-…`). | `supabase/seed.sql` | closed |
| FA-1.12 | ~~**Wyścig przy równoległym podwójnym webhooku Stripe.** Obecna idempotencja to „odczytaj `deposit_paid_at`, jeśli null — zapisz" (check-then-write). Dwa równoległe webhooki mogą obydwa odczytać `null`, obydwa zapisać i obydwa wyemitować `payment.received` + `transition(paid)`. Rozwiązanie atomowe: `UPDATE inquiries SET deposit_paid_at = now(), deposit_stripe_session_id = $2 WHERE id = $1 AND deposit_paid_at IS NULL RETURNING id` — tylko jeden rekord zostaje zwrócony, drugi webhook widzi 0 wierszy i kończy bez emisji~~ — **closed FA-1.16**: `.update().eq().is('deposit_paid_at', null).select('id')` — zdarzenia emitowane tylko gdy zwrócono wiersz; test równoległy (Promise.all) pokazuje 1 vs 2 na starym kodzie | `src/app/api/webhooks/stripe-deposit/route.ts` | closed |
| FA-1.12 | **`markPaymentReceived` — ręczne logowanie wpłaty przez admina (ścieżka zapasowa gdy webhook nie odpali)** — funkcja napisana w FA-1.12, ale nigdy nie podpięta do UI (UnmatchedLinker i UnmatchedPageClient nie mają przycisku ani wywołania). Logika jest gotowa (emit `payment.received` z `source='app'` + `transition(paid)`); potrzebny przycisk na karcie zapytania lub w panelu unmatched | `src/actions/messages.ts` (usunięte w FA-1.12), `src/app/admin/inquiries/unmatched/` | FA-1.13 |
| FA-1.12 | ~~**FA-1.07 cleanup: orphaned `sendMessageToAngler` in `inquiries.ts`**. `sendMessageToAngler` in `src/actions/inquiries.ts` (line 796) inserts into `messages` without emitting a `message.sent` event to `inquiry_events` — violates rule 5. After FA-1.12 UI wiring, `MessageComposer` no longer calls it (`sendMessageFromThread` is used instead), but `InquiryActionPanel.tsx` still imports and calls it (line 21, 99). Remove `sendMessageToAngler` from `inquiries.ts` and update `InquiryActionPanel.tsx` to call `sendMessageFromThread` or remove the now-redundant message form from that component.~~ — **closed FA-1.07**: `sendMessageToAngler` usunięte razem z martwym `InquiryActionPanel.tsx` (0 importerów, `page.tsx` renderuje `ThreadActionsPanel`); `grep -rn sendMessageToAngler src` → 0 | `src/actions/inquiries.ts`, `src/app/admin/inquiries/[id]/` | closed (FA-1.07) |
| FA-1.12 | **`.env.local` zawiera produkcyjny `RESEND_API_KEY` i `RESEND_INBOUND_SECRET`** — uruchomienie `pnpm dev` lokalnie wysyła maile transakcyjne przez produkcyjne konto Resend. Ryzyko: deweloper testujący prawdziwe zapytanie (realny adres email) wyśle potwierdzenie do faktycznego klienta. Naprawka: osobny klucz testowy Resend (lub osobne konto), wpisany w `.env.local.example`; jako domyślne zabezpieczenie dodać `RESEND_DEV_FAKE=1` do `.env.local` — emailAdapter omija Resend, tworzy wiersz w `messages` i emituje `message.sent` normalnie, tylko bez prawdziwej dostawy | `.env.local`, `src/lib/channels/email.ts` (`RESEND_DEV_FAKE` check dodany w proof/FA-1.12-ui-walk), `src/lib/email.ts` (stara ścieżka sendEmail nie sprawdza tego flagi) | FA-1.13 lub osobne zadanie XS |
| ~~FA-1.07~~ | ~~**Na liście endpointów Stripe nie ma żadnego celującego w `/api/webhooks/stripe-deposit`** — dwa event destinations: `brilliant-glow` (Active, `https://fjordanglers.com/api/stripe/webhook`) i `platform-webhook` (Disabled, `https://fjordanglers.com/api/webhooks/stripe` — trasy nie ma w repo); żaden nie celuje w endpoint depozytu~~ — **closed FA-1.16**: endpoint testowy + live na `/api/webhooks/stripe-deposit` — STOP gate pending tj approval (URL, mode, events list) | `src/app/api/webhooks/stripe-deposit/route.ts`, Stripe Dashboard | closed |
| FA-1.07 | **`platform-webhook` (Disabled) celuje w `/api/webhooks/stripe` — trasy o tej ścieżce w repo nie ma.** Wyłączony, więc bez skutku dziś. **FA-1.16 STOP: decyzja tj — opcje: (A) usunąć z dashboardu Stripe (jedno kliknięcie, zero ryzyka — endpoint wyłączony); (B) zostawić wyłączony (harmless). Rekomendacja: A.** | Stripe Dashboard → Webhooks | decyzja tj — STOP przed FA-1.16 close |


| FA-1.13 | ~~**40 pre-existing ESLint errors in FA-1.07/1.08 code** — `GuideAttachmentTab.tsx`, `InquiriesFilters.tsx`, `OfferBuilder.tsx`, `page.tsx` and others~~ — **closed FA-1.08**: 40 → 35 w FA-1.07 (5 w usuniętych plikach) → **0** w FA-1.08. `OfferBuilder.tsx` usunięty w całości (decyzja D2), `GuideAttachmentTab.tsx` i `page.tsx` naprawione | `src/app/admin/inquiries/` | closed (FA-1.08) |
| FA-1.13 | **Manual review of 47 non-E.164 angler_phone rows on prod** — all 47 have Iceland/New Zealand/Other trip_country (none NANP), so the migration backfill SKIPS them and emits RAISE NOTICE with their IDs. After running the migration, retrieve the IDs from Supabase logs and normalise manually (or re-run with the correct country codes). | `supabase/migrations/20260918135418_add_guides_phone_normalise_angler.sql` | after prod migration runs (STOP gate: tj confirms WHATSAPP_APP_SECRET in Vercel) |
| FA-1.05 audit | `deposit_paid_at` pusty dla wszystkich 99 zapytań mimo 10 ze `stage_reached='deposit_paid'` — założenie D1 („webhook Stripe ustawia tę kolumnę") fałszywe; `payment.received` ma 0 historycznych zdarzeń; historia bookingów sprzed stage-1 nieodwracalnie utracona. Do wyjaśnienia: czy webhook przed stage-1 w ogóle pisał tę kolumnę. **FA-1.16 dopisek:** REBUILD_PLAN.md §9 dopisek 19 IX podaje `inquiry_events` = 785 wierszy, w tym **1 z `source='webhook'`** (live, post stage-1 deploy). Odczyt produkcji w FA-1.16 niewykonany (MCP `execute_sql` → permission error). Do weryfikacji: `SELECT type, source, channel, occurred_at FROM inquiry_events WHERE source = 'webhook' ORDER BY occurred_at DESC LIMIT 5` na prod — ustalić, czy pochodzi z `/api/webhooks/stripe-deposit` czy innej trasy. | `inquiries.deposit_paid_at`, `inquiry_events.source` | zamknąć w katalogu metryk: M1/M2/M3/M7 nie mają bazyliny sprzed stage-1; odczyt produkcji do wykonania przez tj |
| FA-1.05 audit | 0 wiadomości z `counterpart='guide'` w `messages` — komunikacja z przewodnikami szła poza systemem przed stage-1; `guide.contacted` i M10 (czas do odpowiedzi przewodnika) startują od zera. | `messages`, `docs/REBUILD_PLAN.md` M10 | odnotować w katalogu metryk: M10 nie ma bazyliny sprzed stage-1 |
| FA-1.05 audit | 24 wiersze `external_offer_sent=true` i `offer_sent_at IS NULL` — oferty wysyłane ręcznie bez rejestracji daty; `offer.presented` ma 1 historyczne zdarzenie (z `offer_sent_at`). | `inquiries.offer_sent_at`, `inquiries.external_offer_sent` | odnotować w katalogu metryk: M7 (czas do oferty) nie ma bazyliny sprzed stage-1 |
| FA-1.05 audit | 7 zapytań ma `inquiries.created_at` późniejsze niż pierwsza wiadomość (do 13 h) — rekord powstawał ręcznie po mailu; metryki czasu (M7) mają liczyć od pierwszej wiadomości, nie od `created_at` — do uwzględnienia w etapie 6. | `inquiries.created_at`, `messages.occurred_at` | etap 6 (metryki) |
| FA-1.13 | **Integracja WhatsApp Cloud API zablokowana po stronie Meta, nie kodu.** Konto tj ma ograniczenie dostępu do reklam obejmujące tworzenie portfolio firmowych (komunikat: „Nie możesz zamieszczać reklam, zarządzać zasobami reklamowymi ani tworzyć kont reklamowych i portfolio firmowych"). Kreator aplikacji Meta na kroku „Business" zwraca „No businesses available". Istniejące portfolio „fan page" (ID 1258409340996834, utworzone 4 VIII 2019) ma pustą nazwę firmy, brak adresu i telefonu, status „Nie zweryfikowano" — i tak nie jest oferowane w kreatorze. Zmienne `WHATSAPP_*` w `.env.local` to zaślepki, nie sekrety. tj złożył odwołanie w Account Quality 18 IX 2026. Alternatywy odrzucone na teraz: konto wspólnika, dostawca BSP (360dialog/Twilio — wymagałby przepisania adaptera). | `src/app/api/webhooks/whatsapp/route.ts`, `src/lib/channels/whatsapp.ts`, `docs/ops/whatsapp-e2e-checklist.md`, `.env.local` | po rozpatrzeniu odwołania Meta: założenie portfolio, aplikacja Meta, testowy numer, przejście checklisty E2E i zamknięcie kryteriów 1 i 5 FA-1.13 |
| FA-1.04 | ~~Kryteria 1 i 4 z "Gotowe, gdy" nieudowodnione — lokalny stack nie ma zasianych zapytań (`SELECT qualified, count(*) FROM inquiries` → 0 rows), więc brak: (1) rozkładu `priority`/`trip_country` i liczby wierszy spełniających O-10; (4) przebiegu E2E klasyfikacja agenta → `qualified='yes'` + wiersz `inquiry_events`. Default `'unknown'` i naruszenie CHECK udowodnione osobno (INSERT + red proof). Świadomie zostawione — seed nie jest w zakresie FA-1.04.~~ — **closed FA-1.10** (20 IX 2026): seed rozszerzony, kryterium 1 i 4 udowodnione SELECT-ami (`docs/proof/FA-1.10/fa-1.04-crit1.out`, `fa-1.04-crit4.out`) | `supabase/seed.sql`, `src/lib/inquiries/qualified.ts` | closed |
| FA-1.04 | **setQualified emituje zdarzenie przy każdej rundzie agenta, nawet gdy wartość się nie zmienia.** Jeśli Round 1 ustawi `qualified='yes'`, a angler odpowie i Round 2 oceni tak samo, powstaną dwa wiersze `inquiry_events` z tym samym typem. Wpływa na liczenie M5/M6 (jeśli metryka liczy zdarzenia, nie wartości). Rozwiązanie: przed `setQualified` odczytać obecną wartość i wywołać go tylko gdy się różni — albo liczyć M5/M6 z kolumny `qualified`, nie ze zdarzeń. | `src/lib/ai/inquiry-agent.ts`, `src/lib/inquiries/qualified.ts` | do rozważenia w FA-1.10 przy projektowaniu metryk — **FA-1.10 (20 IX 2026): M5/M6 na `/admin/weekly` liczone z kolumny `qualified` (D4), więc duplikaty zdarzeń nie wpływają na ekran. Sam problem (dwa `inquiry.qualified_set` przy niezmienionej wartości) zostaje otwarty do etapu 5, gdzie zdarzenia zasilą metryki.** |
| stage-1 paczka 2 (19 IX 2026) | **FA-1.04 + FA-1.05 wdrożone bez incydentu.** `db push` wykonany przez tj 19 IX 2026; 5 SELECT-ów weryfikacyjnych zielone (wyniki w task files). `message.received/webhook = 1` — pierwsze żywe zdarzenie po wdrożeniu, emiter działa. PR #63 stage-1 → main otwarty; merge przez tj. | `docs/tasks/FA-1.04.md`, `docs/tasks/FA-1.05.md` | brak działań — zamknięte |
| stage-1 deploy 19 IX 2026 | **Odroczony drop zastosowany razem z migracjami — ~20 min okno awarii.** `20261001000000` wszedł razem z migracjami 1–9, bo `supabase db push` wyświetla listę wszystkich oczekujących migracji i pyta raz [Y/n] — nie ma sposobu wybrania podzbioru. Bramka STOP istniała wyłącznie jako ludzka instrukcja i zawiodła. Skutek: ~20 min, w których produkcja miała nowy schemat i stary kod (email-inbound, webhook WA, karta zapytania, kwalifikacja AI i panel unmatched zwracały 500). Dane bezpieczne (guard przy dropie zweryfikował pokrycie id przed skasowaniem; 684 wiersze przeniesione). Maile odzyskane przez retry Resend. Formularz działał przez cały czas. **Reguła na przyszłość:** odroczonej migracji nie zostawia się w `supabase/migrations/`. Trzyma się ją poza katalogiem (np. `supabase/migrations-deferred/`) i wkłada dopiero gdy ma pójść. Instrukcja dla człowieka przy interaktywnym promptcie nie jest bramką STOP. Dopisać tę regułę do `docs/05-agent-operations.md` §3. | `supabase/migrations/20261001000000_drop_legacy_after_deploy.sql`, `docs/05-agent-operations.md` §3 | reguła do zastosowania przy każdym kolejnym wdrożeniu etapu |
| stage-1 deploy 19 IX 2026 | **„Require approvals" zablokowała wdrożenie w trakcie okna awarii.** Reguła `required_approving_review_count ≥ 1` na `main` jest nie do spełnienia, gdy tj jest jedyną osobą z prawem zapisu — własnego PR-a nie można zatwierdzić, a bypass list rulesetu tego nie omija, bo wymóg pochodzi z klasycznej ochrony gałęzi. 19 IX zdjęta ręcznie pod presją czasu. **Stan ochrony `main` po 19 IX (odczytany z GitHub API):** PR wymagany (zero wymaganych recenzji — `required_approving_review_count: 0`), status checks w trybie strict ale bez wpisanych nazw checków (`contexts: []`, `checks: []`), force push dozwolony (`allow_force_pushes: true`), admins enforced. **Zalecenie:** zostawić wymóg PR i po wdrożeniu FA-1.11 dopisać konkretne nazwy checków (`CI/check`, `CI/db`, `CI/sync`) do `required_status_checks`; nie przywracać `required_approving_review_count ≥ 1` do czasu, aż w repo będzie druga osoba z prawem zapisu. | GitHub Settings → Branches → `main` protection | tj do sprawdzenia po onboardingu kolejnej osoby |
| FA-1.09 | Fallback depozytu nie obejmuje walut innych niż EUR: `flat`/NZD to 15 z 98 zapytań (drugi koszyk co do wielkości, Nowa Zelandia), `flat`/USD 2, `request`/EUR 6. Te zapytania bez `offer_deposit_eur` dostają błąd „No offer deposit set” i wymagają ręcznej oferty. Przeliczanie kursem świadomie odrzucone — line item Stripe ma zaszyte `eur`, a kurs w momencie zwrotu nie zgadzałby się z kursem wpłaty | `src/actions/inquiries.ts` `sendDepositLink`, `src/lib/inquiries/deposit-fallback.ts` | wielowalutowy Stripe = osobne zadanie, nie wcześniej niż etap 4; do tego czasu NZ obsługiwane ręczną ofertą (liczby z odczytu tj 20 IX 2026) |
| FA-1.09 | Klucze idempotencji Stripe budowane z `Date.now()` (`deposit-${inquiryId}-${Date.now()}`, `offer-deposit-${inquiry.id}-${Date.now()}`) — każdy klik tworzy nową sesję, klucz niczego nie chroni | `src/actions/inquiries.ts` `sendDepositLink` (l. ~342), `submitOfferAnswers` (l. ~681) | podwójny klik = dwa linki płatności dla tego samego zapytania; klucz powinien być stabilny (np. `inquiryId` + kwota + `depositPercent`) |
| FA-1.09 | Mail `deposit-link-angler` drukuje „{depositPercent}% Booking & Curation Fee (€X)” bezwarunkowo, a `sendDepositLink` przekazuje `depositPctUsed = depositPercent` także wtedy, gdy kwota pochodzi z `offer_deposit_eur` — klient widzi np. „30%” obok kwoty, która nie jest 30% niczego. Przed FA-1.06 gałąź oferty ustawiała `depositPctUsed = 0` | `src/actions/inquiries.ts` `sendDepositLink`, `src/emails/deposit-link-angler.tsx` | mylące dla klienta; poprawka = ukryć procent w mailu, gdy kwota z oferty (zmiana treści maila — poza brzegiem FA-1.09) |
| FA-1.09 | `InquiriesCalendar` linkuje do `/trips/${row.trip_id}`, gdy brak sluga; w `src/app/trips` nie ma trasy `[id]` (tylko `page.tsx` i komponenty) — link kończy się 404. Po FA-1.09 slug rozwiązuje się dla prawie każdego zapytania, więc dotyka to niemal wyłącznie zapytań bez experience_pages | `src/app/admin/inquiries/InquiriesCalendar.tsx` (`tripHref`) | FA-1.08 przy wycinaniu propsów `InquiriesCalendar` |
| FA-1.09 | `admin/inquiries/page.tsx`, `admin/inquiries/[id]/page.tsx` i `dashboard/trips/page.tsx` wołają `.from(...)` bezpośrednio (reguła 3: strony nie czytają Supabase). Istniało przed FA-1.09, helper dodany w tym zadaniu nie zmienia reguły dla reszty zapytań w tych plikach | trzy pliki j.w. | przeniesienie do `packages/core` w etapie 2 |
| FA-1.09 | Batch helper `getInquiryExperiences` buduje filtr PostgREST `.or('id.in.(…),trip_id.in.(…)')`; testy jednostkowe mockują klienta, więc składnia i zachowanie na prawdziwym PostgREST są **niezweryfikowane** poza buildem | `src/lib/inquiries/experience-lookup.ts` | sprawdzić po deployu: `/admin/inquiries` pokazuje nazwy wypraw dla zapytań z samym `trip_id` i z samym `experience_page_id` (checklista w raporcie FA-1.09) |
| FA-1.09 | `pnpm` 12.4.2 (Node v22.23.2 z fnm, WSL) przerywa każdy skrypt błędem `ERR_PNPM_IGNORED_BUILDS` i dopisuje do `pnpm-workspace.yaml` blok `allowBuilds` z placeholderami „set this to true or false” (istnieje już `ignoredBuiltDependencies`). Obejście: `./node_modules/.bin/{tsc,vitest,eslint,next}`, a `git checkout -- pnpm-workspace.yaml` po każdym uruchomieniu `pnpm` | `pnpm-workspace.yaml`, środowisko lokalne | inaczej łatwo wcommitować śmieci do workspace'u; do rozstrzygnięcia: `allowBuilds` zamiast `ignoredBuiltDependencies` albo przypięta wersja pnpm |
| FA-1.09 | `getInquiryExperiences` filtruje id przez `UUID_RE` (ochrona stringa `.or()`), a `getInquiryExperience` nie — id spoza kształtu UUID rozwiąże się na karcie zapytania, ale da `—` na liście | `src/lib/inquiries/experience-lookup.ts` | rozjazd zachowania karta vs lista; wyrównać przy okazji weryfikacji filtra na żywym PostgREST |
| FA-1.09 | `getInquiryConfirmation` zmieniona bez testu jednostkowego i bez przebiegu przeciw bazie (`getInquiryConfirmation.test.ts` to test integracyjny, wymaga lokalnego stacku) | `src/actions/inquiries.ts` `getInquiryConfirmation`, `src/actions/getInquiryConfirmation.test.ts` | test do dopisania przy pierwszym zadaniu, które uruchamia lokalny stack |
| FA-1.07 | **`sendOfferEmail` zostaje — pytanie otwarte:** jedyny wołający to `OfferBuilder.tsx:338`, żywy (`ProposalTab.tsx` → `page.tsx:723`), więc usunięcie byłoby zmianą funkcjonalną i drugim dotknięciem `.tsx`. Czy builder ofert zostaje po FA-1.12 (`offers`), czy cała ścieżka `sendOfferEmail` + `saveOfferDraft` + `OfferBuilder` + `OfferBuilderModal` (0 importerów, zostawiony świadomie) + `ProposalTab` idzie do wycięcia? Nie rozstrzygnięte w FA-1.07 | `src/actions/inquiries.ts` (`// TODO FA-1.08`), `src/app/admin/inquiries/[id]/{OfferBuilder,OfferBuilderModal,ProposalTab}.tsx`, `docs/REBUILD_PLAN.md` załącznik C (`offer.created/updated/sent` usunięte z katalogu) | decyzja produktowa tj przed FA-1.08 |
| FA-1.07 | **`api/stripe/webhook` — pusta po FA-1.07**, trzymana wyłącznie dla aktywnego endpointu `brilliant-glow` w Stripe (tylko zdarzenia z kont połączonych, produkt nie ma Connect — ADR-0001). Usunąć trasę i sekret po wyłączeniu tego endpointu w dashboardzie Stripe. → etap 7 | `src/app/api/stripe/webhook/route.ts`, `STRIPE_CONNECT_WEBHOOK_SECRET` w `src/lib/env.ts` | usunięcie trasy przy aktywnym endpoincie = 404 na żywym webhooku (dashboard Stripe, 20 IX 2026, tryb live, error rate 0%) |
| FA-1.07 | **Endpoint Stripe `platform-webhook` (Disabled) celuje w `/api/webhooks/stripe`** — trasy o tej ścieżce w repo nie ma. Zgłoszone z dashboardu Stripe (20 IX 2026), nie diagnozowane w FA-1.07 | dashboard Stripe (właściciel: tj) | wyłączony, więc bez skutku dziś; do posprzątania po stronie Stripe |
| FA-1.07 | **Na liście endpointów Stripe nie ma żadnego celującego w `/api/webhooks/stripe-deposit`** — a to jedyna ścieżka zapisu `deposit_paid_at` (reguła 7: rezerwacja = wpłacony depozyt). Zgłoszone z dashboardu Stripe (20 IX 2026), nie diagnozowane w FA-1.07 | dashboard Stripe (właściciel: tj), `src/app/api/webhooks/stripe-deposit/route.ts` | jeśli faktycznie brak endpointu, żadna wpłata nie zapisze `deposit_paid_at` i nie wyemituje `payment.received`; do zweryfikowania w dashboardzie |
| FA-1.07 | `deleteAccount` (samoobsługowe usunięcie konta, wyjątek udokumentowany w FA-0.06 §196) usunięty w FA-1.07 jako eksport bez wołającego i bez UI. Przywrócić z historii, gdy powstanie ekran samoobsługowego usuwania konta | `src/actions/auth.ts` (usunięte), `docs/tasks/FA-0.06.md` §196 (zapis historyczny, nie edytowany) | decyzja tj z 20 IX 2026 (druga, zmieniająca pierwszą: „zostaje”); `git log -S deleteAccount -- src/actions/auth.ts` wskazuje commit z definicją |
| FA-1.07 → FA-1.08 | ~~**Residuum knipa po FA-1.07 (17 pozycji, `docs/proofs/FA-1.07-knip-after.txt`)**: `OfferBuilderModal.tsx`; nieużywane eksporty `NoGuideContactCard`, `TripOptionsAccordion`, `CropPreview`, typy `TripDetails`, `GuideFormDefaults`, `GuideEditData`, `OptionTabConfig`, `HelpItem`, sześć stałych w `src/emails/_shared.tsx`, duplikat `default` w `inquiry-agent-email.tsx`; nieaktualne komentarze (`AssignGuidePanel`, `GuideOnboarding`, `GuideSubmissionForm`, `ExperiencePageWithOptions`); martwy CSS `[cmdk-item]`~~ — **closed FA-1.08**: wszystkie 17 pozycji zdjęte, `pnpm knip` → 0 (`docs/proofs/FA-1.08-knip-after.txt`), krok `knip` w `ci.yml` stracił `continue-on-error` | patrz lista | closed (FA-1.08) |
| FA-1.07 | Lokalne środowisko (WSL): (1) `node_modules/supabase/bin/supabase` nie istnieje (skrypty budowania pominięte przez `ignoredBuiltDependencies`), więc `pnpm exec supabase` pada — stack startował przez `npx supabase@2.75.0` (wersja z CI); (2) `pnpm` 12 odrzuca świeże wydania (`minimumReleaseAge`, np. `formatly@0.7.1`) i przepisuje lockfile — lockfile generowany `pnpm` 10.30.3 z `--config.minimumReleaseAge=1440`, dowód `docs/proofs/FA-1.07-lockfile-proof.txt`; (3) `tsconfig.json` włącza `.next/types/**`, więc po zmianie gałęzi `pnpm typecheck` widzi nieaktualne typy (`admin/weekly/page.js`) — pomaga `rm -rf .next/types .next/dev/types`. Powiązane z wierszem FA-1.09 o `pnpm` 12 | `package.json` (brak `packageManager`), `tsconfig.json`, `pnpm-workspace.yaml` | przypiąć wersję pnpm (`packageManager`) i dopisać do `docs/05-agent-operations.md` §9, że §9 opisuje macOS/OrbStack, nie WSL |
| FA-1.10 | `spendEur` / `avgCpcEur` w `fetch-campaigns.ts` to w rzeczywistości PLN (waluta rozliczeniowa konta Google Ads; `ad_campaigns.spend` trzyma złotówki, `AdsClient.tsx:409` renderuje to przez `fmtPln`) — zmienić nazwy przy najbliższym dotknięciu Google Ads (decyzja tj T1, 20 IX) | `src/lib/google-ads/fetch-campaigns.ts:61` (+ konsumenci w cronie `sync-google-ads`) | mylące nazwy; ktoś „naprawi” to mnożąc przez kurs EUR→PLN i zawyży wydatek ~4,3× |
| FA-1.10 | **`/admin/finances` pomija zapytania ze statusem `paid` i `handed_over`.** Zapytanie o przychód filtruje `status IN ('deposit_paid','completed')` — słownik sprzed FA-1.03; migracja `20260916201225` przemapowała `deposit_paid → paid`, a webhook Stripe ustawia dziś `paid` przez `transition()`. Na seedzie FA-1.10: `/admin/finances` 1548 zł (€360), `/admin/weekly` 2838 zł; różnica 1290 zł = jedno zapytanie `paid` (300 € × 4,30). Po ustawieniu tego zapytania na `completed` obie strony pokazują 2838 zł. Drugi rozjazd: finances przypisuje wiersz do miesiąca po `(deposit_paid_at ?? updated_at).slice(0,7)` (UTC, z fallbackiem na `updated_at`), weekly po strefie Europe/Warsaw i wyłącznie `deposit_paid_at` | `src/app/admin/finances/page.tsx:99` | tj wybrał opcję A (20 IX): FA-1.10 przepina finances tylko na wspólny `commissionPln`, filtr wierszy zostaje. Zadanie S: filtr → `deposit_paid_at IS NOT NULL` (zmieni wyświetlaną liczbę na prod — pokazać różnicę przed wdrożeniem). Na prod `deposit_paid_at` jest dziś puste dla wszystkich 99 zapytań (wiersz FA-1.05 audit), więc do czasu pierwszej wpłaty po stage-1 oba ekrany pokażą ~0 |
| FA-1.10 | `sendInquiryAgentEmail` i całe `src/lib/email.ts` nie mają trybu dev-fake (`RESEND_DEV_FAKE` sprawdza tylko `channels/email.ts` i `email-inbound`). W `runAgentRound1` wysyłka maila **poprzedza** zapis klasyfikacji (`update` z `classUpdate`, `setQualified`) — błąd Resend (401 z fałszywym kluczem) przerywa rundę i klasyfikacja/`qualified` przepadają, mimo komentarza „save classification regardless”. Lokalnie: przebieg agenta wymaga stubu `fetch` dla `api.resend.com` (preload `NODE_OPTIONS=--require`), inaczej `agent_round=0`, `qualified='unknown'` | `src/lib/ai/inquiry-agent.ts` (`runAgentRound1`, ok. l. 429–457), `src/lib/email.ts:104` | S: zapisać klasyfikację przed wysyłką (albo w `finally`) i dodać `RESEND_DEV_FAKE` do `email.ts` — powiązane z wierszem FA-1.12 o `.env.local` z kluczem Resend |
| FA-1.10 | Agent wstawia wiadomość wychodzącą do `messages` bezpośrednio (`.from('messages').insert` z `as any`, `inquiry-agent.ts` Round 1 i 2) bez `message.sent` w `inquiry_events` — przebieg lokalny FA-1.10: zapytanie testowe ma tylko `inquiry.created` i `inquiry.qualified_set`, a agent wysłał pytanie kwalifikujące. Reguła 5 (zmiana stanu = zdarzenie) i reguła 8 (`as any`) | `src/lib/ai/inquiry-agent.ts` (Round 1 ok. l. 419, 470; Round 2 analogicznie) | M10–M12 nie zobaczą pierwszej odpowiedzi agenta; naprawić przez `sendMessageFromThread`/wspólną ścieżkę wysyłki — osobne zadanie S/M |
| FA-1.10 | `dev.sh` ładuje `.env.local` (produkcja) do powłoki i uruchamia `claude`; zmienne już obecne w środowisku procesu wygrywają z każdym plikiem `.env*`, więc `.env.development.local` nie ochroni dev-serwera uruchomionego z takiej powłoki. Sam `pnpm dev` ładuje `.env.local` bez udziału `dev.sh` | `dev.sh`, `.env.development.local` (gitignorowany, README) | README opisuje zasadę; docelowo `dev.sh` nie powinien eksportować sekretów produkcji do sesji agenta (O-11) |
| FA-1.10 | `commissionPln` odtwarza dzisiejszą formułę na `NUMERIC` euro (float) — zgodnie z D2 i po to, by `/admin/finances` nie zmienił liczby — więc reguła 6 (integer cents, kurs zamrożony) nie jest tu spełniona. Poprawna definicja (`deals.commission_cents`, kurs zamrożony w chwili zdarzenia) to etap 4; helper i `/admin/weekly` są do wyrzucenia | `src/lib/metrics/commission.ts` | świadomy wyjątek od reguły 6, opisany w nagłówku pliku |
| FA-1.10 | Na produkcji nie zweryfikowano skali `ad_campaigns.spend` (bramka STOP T1): MCP Supabase zwraca „no permission”, w sesji nie było hasła bazy. Decyzja T1 (PLN) oparta o kod (`AdsClient` → `fmtPln`, sync zapisuje walutę konta) i słowo tj, nie o odczyt | `ad_campaigns` (produkcja) | jeden SELECT `min/max/sum(spend)` i porównanie z panelem Google Ads przy najbliższym odczycie prod |
| FA-1.07 | `scripts/agent-guard.sh` blokuje polecenia, które nie są force-pushem: wzorzec force-pusha (`git push`, dowolny ciąg, `-f`) dopasowuje `-F` z `gh api -F body=@plik` — dopasowanie bez rozróżniania wielkości liter, a kropka przechodzi przez całe wielowierszowe polecenie. Blokuje nawet polecenie, które tylko *zawiera tekst* tego wzorca (np. wiersz do tego pliku wklejony przez `Bash`). Skutek: agent rozbija polecenie na dwa albo używa `Edit`. Poprawka: wzorzec case-sensitive i zawężony do jednego polecenia `git push` (kilka znaków) | `scripts/agent-guard.sh` | rekomendacja tj z 20 IX: doklejka do FA-1.08 (dotyka CI i narzędzi); plik poza zakresem FA-1.07 |
| FA-1.13 | Kompozytor ma tylko przełącznik angler/guide, widoczny przy `guideAssigned`; brak listy przewodników do wyboru rozmówcy „przewodnik” (zakres pierwotny: „lista + numer”). Adresat: FA-1.15 | `src/app/admin/inquiries/[id]/MessageComposer.tsx` | bez listy WA do przewodnika idzie wyłącznie do przypisanego; kontakt z nieprzypisanym przewodnikiem z wątku nie jest możliwy |
| FA-1.13 | `messages.media` trzyma ID medium z Meta (`parseInbound` zapisuje `{type,id}`), nie link: ID Graph API bez tokena jest nierozwiązywalne, więc załącznik WA w wątku nie da się otworzyć. Adresat: FA-1.15, do odblokowania po rozpatrzeniu odwołania Meta (potrzebny token, by pobrać URL i zapisać kopię) | `src/lib/channels/whatsapp.ts` (`parseInbound`), `messages.media` | kryterium „przychodzące tylko jako link w `media`” z FA-1.13 jest spełnione formalnie, nie użytkowo |
| FA-1.13 (runda po przeglądzie) | Rola `authenticated` nie ma testu dostępu do `guide_contacts`: test RLS sprawdza tylko `anon` i `service_role`, bo CI startuje bez gotrue (`-x gotrue,…`), więc nie ma jak zalogować użytkownika. Brak dostępu `authenticated` opiera się na `REVOKE ALL … FROM anon, authenticated` i braku polityki — nieprzetestowany. Przewodnicy logują się do `/dashboard`, więc `authenticated` to realna rola w tej aplikacji. Adresat: FA-1.15 albo osobne XS, gdy CI dostanie gotrue | `src/lib/__tests__/guideContactsRls.test.ts`, `.github/workflows/ci.yml` (job `db`) | numer przewodnika mógłby być czytelny dla zalogowanego przewodnika/anglera, gdyby ktoś dodał politykę albo GRANT — nic tego dziś nie złapie |
| FA-1.13 (runda po przeglądzie) | ~~`Date.now()` w `AdminInquiryDetailPage` (`yearAhead`, ~linia 205) jest nieczyste w renderze; zostawione z jawnym `eslint-disable-next-line` i komentarzem „patrz FA-1.15"~~ — **closed FA-1.08** (decyzja tj, opcja A, 20 IX): `src/lib/availability-window.ts` czyta zegar **raz** i zwraca `{ from, to }`; wołają go `admin/inquiries/[id]`, `dashboard/calendar` i `experiences/[slug]`. `eslint-disable` usunięty, żaden nowy nie dodany. Przy okazji znika realna usterka: `today` i `yearAhead` były dwoma osobnymi odczytami zegara i mogły rozjechać się o dobę na granicy północy | `src/lib/availability-window.ts`, trzy strony | closed (FA-1.08); FA-1.15 nie dziedziczy tego punktu |
| FA-1.08 | **`inquiries.offer_*` nie ma już żadnego pisarza.** Decyzja D2 (tj, 20 IX) usunęła builder ofert (`ProposalTab`, `OfferBuilder`, `LocationPicker`, `saveOfferDraft`, `sendOfferEmail`, `uploadOfferPhoto`, mail `inquiry-rich-offer-angler`). `/offers/[token]` zostaje i nadal czyta te kolumny przez `getOfferByToken`, ale nikt nie potrafi wygenerować nowego tokenu — istniejące linki działają, nowych nie ma. `offers`/`offer_options` z FA-1.12 nie mają odpowiedników dla `offer_trip_plan`, `offer_what_to_bring`, `offer_schedule`, `offer_photos`, `offer_license_info`/`_heading`, `offer_questions`/`_answers`, `offer_location*` (mapa), `offer_refund_reason` | `src/actions/inquiries.ts` (`getOfferByToken`, `acceptOffer`, `declineOffer`, `submitOfferAnswers`), `src/app/offers/[token]/` | **etap 4**: przenieść te pola na `offers`/`offer_options` i odtworzyć kreator na wątku, albo świadomie zrezygnować ze strony oferty. Do tego czasu ofertę przedstawia się wiadomością z wątku (FA-1.12), a depozyt linkiem `createPaymentLink` |
| FA-1.08 | `sendDepositLink` nie ma wołającego w UI — jedynym importerem jest jego własny test (`src/actions/__tests__/sendDepositLink.test.ts`), dlatego knip go nie widzi jako martwego. Stan sprzed tej paczki: builder ofert też go nie wołał. FA-1.09 włożyło w niego fallback depozytu i 6 testów | `src/actions/inquiries.ts` (`sendDepositLink`), `src/lib/inquiries/deposit-fallback.ts` | albo wrócić z przyciskiem „wyślij link do depozytu" na kartę, albo usunąć akcję razem z fallbackiem i testami — dziś to kod utrzymywany bez użytkownika. Decyzja tj, nie agenta |
| FA-1.08 | `src/components/trips/TripOptionsAccordion.tsx` nie zawiera już komponentu `TripOptionsAccordion` (usunięty jako nieużywany) — plik eksportuje `OptionPanel` i typ `TripOption`. Nazwa pliku wprowadza w błąd | `src/components/trips/TripOptionsAccordion.tsx` + 3 importerów | zadanie XS: `git mv` na `option-panel.tsx` i poprawka trzech importów; nie zrobione tutaj, żeby nie mieszać przenosin plików do paczki o martwym kodzie |
| FA-1.08 | Żaden kod w `src/` nie wstawia wierszy do `guide_submissions` — publiczny formularz zgłoszeń zniknął razem z lejkiem przewodników w FA-1.06. `/admin/submissions` i `/admin/experiences/new` czytają tabelę, `markSubmissionInProgress` ją aktualizuje; wiersze są historyczne. Docblock `/admin/submissions` mówił „Guides fill GuideSubmissionForm" — poprawiony w tej paczce, sama tabela nie | `src/app/admin/submissions/`, `src/actions/submissions.ts`, `public.guide_submissions` | etap 4 razem z decyzją o lejku przewodników (patrz wiersz FA-1.06 o `/guides/apply`) |
| FA-1.08 | `whatsapp-bridge/poll-emails.mjs` nie parsował się (przykład crona PM2 zawiera `*/`, co zamykało komentarz blokowy) — `node --check` czerwony, więc poller Zoho **nigdy nie mógł wystartować pod PM2**. Składnia naprawiona w tej paczce, ale nikt nie sprawdził, czy ten poller jest jeszcze potrzebny po FA-1.12 (inbound e-mail idzie webhookiem Resend) ani czy działa | `whatsapp-bridge/poll-emails.mjs`, `whatsapp-bridge/import-emails.mjs` | zadanie XS: ustalić z tj, czy `whatsapp-bridge/` żyje; jeśli nie — usunąć katalog w całości, jeśli tak — uruchomić i opisać w `docs/ops/` |
